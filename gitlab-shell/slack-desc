# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

            |-----handy-ruler------------------------------------------------------|
gitlab-shell: GitLab Shell (GitLab Shell handles git commands for GitLab)
gitlab-shell:
gitlab-shell: GitLab Shell handles git commands for GitLab and modifies the list of
gitlab-shell: authorized keys. GitLab Shell is not a Unix shell nor a replacement
gitlab-shell: for Bash or Zsh.
gitlab-shell: 
gitlab-shell: Homepage: https://gitlab.com/
gitlab-shell:
gitlab-shell:
gitlab-shell:
gitlab-shell:
